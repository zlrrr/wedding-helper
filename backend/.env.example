# Wedding Helper Backend - Environment Variables Example
# Copy this file to .env and fill in your actual values

# ===== Server Configuration =====
BACKEND_PORT=5001
NODE_ENV=development

# ===== Security Secrets =====
# IMPORTANT: Change these in production! Use strong random strings
JWT_SECRET=your-jwt-secret-change-in-production
SESSION_SECRET=your-session-secret-change-in-production

# ===== CORS Configuration =====
FRONTEND_URL=http://localhost:3000
CORS_ORIGIN=http://localhost:3000

# ===== Default Admin User =====
# Admin user will be created automatically on first startup
DEFAULT_ADMIN_USERNAME=admin
DEFAULT_ADMIN_PASSWORD=admin123

# ===== LLM Configuration =====
# Provider options: openai, anthropic, gemini, lm-studio, custom
LLM_PROVIDER=gemini

# OpenAI Configuration (if LLM_PROVIDER=openai)
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o-mini
OPENAI_API_URL=https://api.openai.com/v1

# Anthropic Configuration (if LLM_PROVIDER=anthropic)
ANTHROPIC_API_KEY=sk-ant-...
ANTHROPIC_MODEL=claude-3-5-sonnet-20241022
ANTHROPIC_API_URL=https://api.anthropic.com/v1

# Gemini Configuration (if LLM_PROVIDER=gemini)
GEMINI_API_KEY=your-gemini-api-key-here
GEMINI_MODEL=gemini-1.5-flash
GEMINI_API_URL=https://generativelanguage.googleapis.com/v1beta

# LM Studio Configuration (if LLM_PROVIDER=lm-studio)
LM_STUDIO_API_URL=http://localhost:1234/v1
LM_STUDIO_MODEL=local-model

# Custom LLM Configuration (if LLM_PROVIDER=custom)
CUSTOM_LLM_API_URL=http://your-custom-llm-endpoint
CUSTOM_LLM_API_KEY=your-custom-api-key
CUSTOM_LLM_MODEL=your-model-name

# LLM General Settings
LLM_TEMPERATURE=0.7
LLM_MAX_TOKENS=2000

# ===== Database Configuration =====
# SQLite database file path (relative to backend root)
DATABASE_PATH=data/wedding-helper.db

# ===== File Upload Configuration =====
UPLOAD_DIR=uploads
MAX_FILE_SIZE=10485760
# 10MB in bytes

# ===== Logging Configuration =====
LOG_LEVEL=info
# Options: error, warn, info, http, debug

# ===== RAG Configuration =====
# Chunk size for text splitting
CHUNK_SIZE=500
CHUNK_OVERLAP=50

# Top K results for RAG retrieval
RAG_TOP_K=3

# ===== Optional: Embedding Service =====
# If you want to use embeddings for better RAG
# EMBEDDING_PROVIDER=openai
# EMBEDDING_MODEL=text-embedding-3-small
# EMBEDDING_API_KEY=sk-...
